{
  "name": "swu-singles-nz",
  "version": "0.1.0",
  "private": true,
  "type": "module",

  "scripts": {
    "build": "npm run app:build && npm run worker:typecheck",
    "app:dev": "vite --host 0.0.0.0",
    "app:typecheck": "vue-tsc -p tsconfig.app.json --noEmit",
    "app:build": "npm run app:typecheck && vite build",
    "app:preview": "vite preview --host 0.0.0.0",
    "worker:typecheck": "tsc -p tsconfig.worker.json --noEmit",
    "worker:dev": "wrangler dev src/worker/index.ts",
    "worker:deploy": "wrangler deploy src/worker/index.ts",
    "db:migrate": "wrangler d1 migrations apply swu-singles-nz",
    "db:migrate:remote": "wrangler d1 migrations apply swu-singles-nz --remote",
    "cards:import:official": "node scripts/import-card-set.mjs"
  },
  
  "dependencies": {
    "@vitejs/plugin-vue": "latest",
    "vite": "latest",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "vue-tsc": "latest",
    "wrangler": "latest"
  }
}
