import { createStorepassAdapter } from '../integrations/storepassIntegration';

export const calicoKeepAdapter = createStorepassAdapter({
  key: 'calicokeep-storepass',
  sellerName: 'Calico Keep',
  storeId: 'MInamaYs3W',
  productLine: 'Star Wars Unlimited'
});
